# Teacher AMS Frontend

Scaffolded Vite + React + TypeScript app.

Dev server:

```bash
cd frontend
npm install
npm run dev
```

Set `VITE_API_URL` in `.env.local` if your backend is not at `http://127.0.0.1:8000`.
